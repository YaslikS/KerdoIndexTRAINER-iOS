import Foundation

enum Strings: String {
    case usersTableStr = "users"
    case jsonEmptyFieldStr = "empty"
    case jsonFieldStr = "json"
    case lastDateFieldStr = "lastDate"
    case nameFieldStr = "name"
    case iconUrlFieldStr = "iconUrl"
    case typeFieldStr = "type"
    case emailFieldStr = "email"
}
